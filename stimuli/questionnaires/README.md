# Questionnaire Placeholders

Put the learning-cycle questionnaire files here.

The current learning-cycle condition table resolves these fields relative to this folder:

- `pretest_form`
- `rating_form`
- `posttest_form`

The current task only shows placeholder screens and records the referenced file names.
Later, the real questionnaire implementation can load the files from this folder.
